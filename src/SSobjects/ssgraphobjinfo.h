#ifndef SSABSTRACTGRAPHOBJ_H
#define SSABSTRACTGRAPHOBJ_H
/*
#include <QObject>
#include <QVector>

struct singleState{
   bool StateFrameisCycled;
   int StateFrameDelay;
   QString StateFrame[10];
   bool StateFrame_Transparent[10];
   singleState():StateFrameDelay(0),StateFrameisCycled(0){}
};

class SSGraphicData : public QObject
{
    Q_OBJECT

public:
    explicit SSGraphicData(QObject *parent = 0);
private:
    QVector<singleState> states;

signals:

public slots:
};

//на будущее
class SSAbstractGraphObj : public QObject
{
    Q_OBJECT
public:
    explicit SSAbstractGraphObj(const QPixmap &pixmap,QObject *parent = 0);

signals:

public slots:
};
*/
#endif // SSABSTRACTGRAPHOBJ_H
